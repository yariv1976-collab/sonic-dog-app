{
  "name": "sonic-dog-app",
  "version": "1.0.0",
  "description": "Sonic family dog manager with push notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate-keys": "node -e \"const wp=require('web-push');const k=wp.generateVAPIDKeys();console.log('VAPID_PUBLIC='+k.publicKey);console.log('VAPID_PRIVATE='+k.privateKey);\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "web-push": "^3.6.7",
    "node-cron": "^3.0.3"
  }
}
